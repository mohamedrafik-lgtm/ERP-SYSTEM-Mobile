# 🔍 تم إضافة Debug Logs لحل مشكلة تحميل الجدول الدراسي

## 🎯 المشكلة:

**"فشل في تحميل الجدول الدراسي"** عند الدخول على صفحة الجدول الدراسي

## 🔧 الحل المطبق:

### **1. إضافة Debug Logs شاملة** ✅
- في `ScheduleDetailsScreen` لمراقبة الـ API call
- في `SemesterSelectionScreen` لمراقبة الـ navigation
- في `AuthService.getClassroomSchedule` لمراقبة الـ API response

### **2. تحسين Error Handling** ✅
- عرض رسائل خطأ أكثر تفصيلاً
- التحقق من صحة الـ `classroomId`
- معالجة مختلف هياكل الـ response

### **3. إضافة Fallback Mechanisms** ✅
- التحقق من وجود `classroomId` قبل استدعاء الـ API
- معالجة مختلف هياكل البيانات المرجعة
- البحث عن البيانات في مفاتيح مختلفة

## 🔍 Debug Logs المضافة:

### **1. ScheduleDetailsScreen** ✅
```javascript
console.log('🔍 ScheduleDetailsScreen - Component loaded with params:', {
  programId: programId,
  programName: programName,
  classroomId: classroomId,
  classroomName: classroomName,
  classroomIdType: typeof classroomId,
  classroomIdValue: classroomId
});

console.log('🔍 ScheduleDetailsScreen - Fetching classroom schedule for classroomId:', classroomId);
console.log('🔍 ScheduleDetailsScreen - classroomId type:', typeof classroomId);
console.log('🔍 ScheduleDetailsScreen - classroomId value:', classroomId);

console.log('🔍 ScheduleDetailsScreen - Classroom schedule response:', JSON.stringify(response, null, 2));
console.log('🔍 ScheduleDetailsScreen - Response type:', typeof response);
console.log('🔍 ScheduleDetailsScreen - Is response array?', Array.isArray(response));
```

### **2. SemesterSelectionScreen** ✅
```javascript
console.log('🔍 SemesterSelectionScreen - Classroom pressed:', {
  id: classroom.id,
  name: classroom.name,
  classNumber: classroom.classNumber,
  startDate: classroom.startDate,
  endDate: classroom.endDate
});

console.log('🔍 SemesterSelectionScreen - Navigating to schedule details with:', {
  programId: programId,
  programName: programName,
  classroomId: classroom.id,
  classroomName: classroom.name
});
```

### **3. AuthService** ✅
```javascript
console.log('🔍 AuthService.getClassroomSchedule() - Fetching classroom schedule for ID:', classroomId);
console.log('🔍 AuthService.getClassroomSchedule() - URL:', url);
console.log('🔍 AuthService.getClassroomSchedule() - Response status:', response.status);
console.log('🔍 AuthService.getClassroomSchedule() - Response:', data);
```

## 📱 كيفية التشخيص:

### **1. افتح Developer Console**
- اضغط F12 في المتصفح
- أو استخدم React Native Debugger
- أو استخدم Metro bundler logs

### **2. اتبع التدفق التالي:**
1. **افتح صفحة الجداول الدراسية**
2. **اختر برنامج**
3. **اختر فصل دراسي**
4. **راقب الـ console logs**

### **3. راقب الـ Logs التالية:**

#### **أ. في SemesterSelectionScreen:**
```
🔍 SemesterSelectionScreen - Classroom pressed: {id: X, name: "Y", ...}
🔍 SemesterSelectionScreen - Navigating to schedule details with: {...}
```

#### **ب. في ScheduleDetailsScreen:**
```
🔍 ScheduleDetailsScreen - Component loaded with params: {...}
🔍 ScheduleDetailsScreen - classroomId exists, fetching schedule slots
🔍 ScheduleDetailsScreen - Fetching classroom schedule for classroomId: X
```

#### **ج. في AuthService:**
```
🔍 AuthService.getClassroomSchedule() - Fetching classroom schedule for ID: X
🔍 AuthService.getClassroomSchedule() - URL: [URL]
🔍 AuthService.getClassroomSchedule() - Response status: [STATUS]
🔍 AuthService.getClassroomSchedule() - Response: [DATA]
```

#### **د. في ScheduleDetailsScreen (معالجة البيانات):**
```
🔍 ScheduleDetailsScreen - Classroom schedule response: [DATA]
🔍 ScheduleDetailsScreen - Response type: [TYPE]
🔍 ScheduleDetailsScreen - Is response array? [BOOLEAN]
🔍 ScheduleDetailsScreen - Final scheduleData: [DATA]
🔍 ScheduleDetailsScreen - ScheduleData length: [NUMBER]
```

## 🚨 المشاكل المحتملة والحلول:

### **1. مشكلة في الـ classroomId**
**الأعراض:**
```
🔍 ScheduleDetailsScreen - classroomId is missing or invalid: undefined
```

**الحل:**
- تحقق من أن الـ `classroom.id` موجود في `SemesterSelectionScreen`
- تحقق من أن الـ navigation parameters يتم تمريرها بشكل صحيح

### **2. مشكلة في الـ API Call**
**الأعراض:**
```
🔍 AuthService.getClassroomSchedule() - Response status: 404
🔍 AuthService.getClassroomSchedule() - Error response: [ERROR_MESSAGE]
```

**الحل:**
- تحقق من أن الـ `classroomId` صحيح
- تحقق من أن الـ API endpoint يعمل
- تحقق من الـ authentication token

### **3. مشكلة في معالجة البيانات**
**الأعراض:**
```
🔍 ScheduleDetailsScreen - Response is null or undefined
🔍 ScheduleDetailsScreen - Unknown response structure, trying to extract data
```

**الحل:**
- تحقق من هيكل البيانات المرجعة من الـ API
- تحقق من أن الـ API يرجع البيانات بالشكل المتوقع

### **4. مشكلة في الـ Authentication**
**الأعراض:**
```
🔍 AuthService.getClassroomSchedule() - Response status: 401
🔍 AuthService.getClassroomSchedule() - Error response: Unauthorized
```

**الحل:**
- تحقق من الـ JWT token
- تأكد من تسجيل الدخول
- جرب تسجيل الدخول مرة أخرى

## 🔧 التحسينات المضافة:

### **1. Error Handling محسن** ✅
```javascript
Alert.alert('خطأ', `فشل في تحميل الجدول الدراسي: ${error.message}`);
```

### **2. Validation للـ Parameters** ✅
```javascript
if (classroomId) {
  console.log('🔍 ScheduleDetailsScreen - classroomId exists, fetching schedule slots');
  fetchScheduleSlots();
} else {
  console.log('🔍 ScheduleDetailsScreen - classroomId is missing or invalid:', classroomId);
  Alert.alert('خطأ', 'معرف الفصل الدراسي غير صحيح');
  setLoading(false);
}
```

### **3. معالجة هياكل البيانات المختلفة** ✅
```javascript
if (Array.isArray(response)) {
  scheduleData = response;
} else if (response.data) {
  scheduleData = Array.isArray(response.data) ? response.data : response.data.slots || response.data.schedule || [];
} else if (response.schedule) {
  scheduleData = Array.isArray(response.schedule) ? response.schedule : [];
} else {
  // Try to find any array in the response
  const responseKeys = Object.keys(response);
  for (const key of responseKeys) {
    if (Array.isArray(response[key])) {
      scheduleData = response[key];
      break;
    }
  }
}
```

## 📋 قائمة التحقق:

- [ ] فتح Developer Console
- [ ] فتح صفحة الجداول الدراسية
- [ ] اختيار برنامج
- [ ] اختيار فصل دراسي
- [ ] مراقبة الـ console logs
- [ ] تحديد المشكلة بناءً على الـ logs
- [ ] تطبيق الحل المناسب

## 🎯 الخطوات التالية:

### **1. بعد مراجعة الـ Logs:**
- إذا كانت المشكلة في الـ `classroomId` → تحقق من الـ navigation
- إذا كانت المشكلة في الـ API → تحقق من الـ backend
- إذا كانت المشكلة في البيانات → تحقق من هيكل الـ response

### **2. إذا كانت المشكلة في الـ Backend:**
- تحقق من أن `/api/schedule/classroom/{classroomId}` يعمل
- تحقق من أن الـ database يحتوي على بيانات الجدول الدراسي
- تحقق من الـ authentication

### **3. إذا كانت المشكلة في الـ Frontend:**
- تحقق من الـ navigation parameters
- تحقق من معالجة البيانات
- تحقق من الـ error handling

---

## 🔍 الآن افتح الصفحة وراقب الـ console logs!

**الـ debug logs ستظهر لنا:**
1. **هل الـ classroomId يتم تمريره بشكل صحيح؟**
2. **هل الـ API call يتم بنجاح؟**
3. **ما هو هيكل البيانات المرجعة؟**
4. **أين تحدث المشكلة بالضبط؟**

**بعد مراجعة الـ logs، أخبرني بالنتائج وسأتمكن من تحديد المشكلة وإصلاحها! 🚀**
