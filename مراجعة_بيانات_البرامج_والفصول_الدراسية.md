# 🔍 تم إضافة Debug Logs لمراجعة بيانات البرامج والفصول الدراسية

## 🎯 الهدف من المراجعة:

### **1. التأكد من البيانات الحقيقية** ✅
- مراجعة الـ response من `/api/programs`
- التأكد من وجود `classrooms` في كل برنامج
- التحقق من أن الفصول الدراسية ليست static data

### **2. Debug Logs المضافة** ✅
- في `AuthService.getAllPrograms()`
- في `ScheduleScreen.fetchPrograms()`
- في `SemesterSelectionScreen.useEffect()`
- في `ScheduleScreen.handleProgramPress()`

## 🔧 Debug Logs المضافة:

### **1. AuthService.getAllPrograms()** ✅
```javascript
console.log('🔍 AuthService.getAllPrograms() - API URL:', `${baseUrl}/api/programs`);
console.log('🔍 AuthService.getAllPrograms() - Response status:', response.status);
console.log('🔍 AuthService.getAllPrograms() - Raw response data:', JSON.stringify(data, null, 2));

// Debug: Check if data contains classrooms
if (Array.isArray(data)) {
  data.forEach((program, index) => {
    console.log(`🔍 AuthService.getAllPrograms() - Program ${index + 1}:`, {
      id: program.id,
      nameAr: program.nameAr,
      hasClassrooms: !!program.classrooms,
      classroomsLength: program.classrooms?.length || 0,
      classrooms: program.classrooms
    });
  });
}
```

### **2. ScheduleScreen.fetchPrograms()** ✅
```javascript
console.log('🔍 ScheduleScreen - Raw API response:', JSON.stringify(response, null, 2));

// Debug: Check if programs have classrooms
programsData.forEach((program, index) => {
  console.log(`🔍 ScheduleScreen - Program ${index + 1}:`, {
    id: program.id,
    nameAr: program.nameAr,
    classrooms: program.classrooms,
    classroomsLength: program.classrooms?.length || 0
  });
  
  if (program.classrooms && program.classrooms.length > 0) {
    console.log(`🔍 ScheduleScreen - Program ${program.nameAr} classrooms:`, program.classrooms);
  } else {
    console.log(`🔍 ScheduleScreen - Program ${program.nameAr} has NO classrooms!`);
  }
});
```

### **3. SemesterSelectionScreen.useEffect()** ✅
```javascript
console.log('🔍 SemesterSelectionScreen - Received classrooms:', JSON.stringify(classrooms, null, 2));
console.log('🔍 SemesterSelectionScreen - Classrooms length:', classrooms?.length || 0);
```

### **4. ScheduleScreen.handleProgramPress()** ✅
```javascript
console.log('🔍 ScheduleScreen - Program pressed:', {
  id: program.id,
  nameAr: program.nameAr,
  classrooms: program.classrooms,
  classroomsLength: program.classrooms?.length || 0
});

if (!program.classrooms || program.classrooms.length === 0) {
  Alert.alert(
    'تحذير', 
    `البرنامج "${program.nameAr}" لا يحتوي على فصول دراسية متاحة.`,
    [{ text: 'موافق' }]
  );
  return;
}

console.log('🔍 ScheduleScreen - Navigating to semester selection with classrooms:', program.classrooms);
```

## 📱 كيفية المراجعة:

### **1. افتح Developer Console**
- اضغط F12 في المتصفح
- أو استخدم React Native Debugger
- أو استخدم Metro bundler logs

### **2. افتح صفحة الجداول الدراسية**
- انتقل إلى `ScheduleScreen`
- راقب الـ console logs

### **3. راقب الـ Logs التالية:**

#### **أ. AuthService Logs:**
```
🔍 AuthService.getAllPrograms() - API URL: [URL]
🔍 AuthService.getAllPrograms() - Response status: [STATUS]
🔍 AuthService.getAllPrograms() - Raw response data: [DATA]
🔍 AuthService.getAllPrograms() - Program 1: [PROGRAM_DATA]
```

#### **ب. ScheduleScreen Logs:**
```
🔍 ScheduleScreen - Raw API response: [DATA]
🔍 ScheduleScreen - Program 1: [PROGRAM_DATA]
🔍 ScheduleScreen - Program [NAME] classrooms: [CLASSROOMS]
🔍 ScheduleScreen - Program [NAME] has NO classrooms!
```

#### **ج. SemesterSelectionScreen Logs:**
```
🔍 SemesterSelectionScreen - Received classrooms: [CLASSROOMS]
🔍 SemesterSelectionScreen - Classrooms length: [LENGTH]
```

#### **د. Navigation Logs:**
```
🔍 ScheduleScreen - Program pressed: [PROGRAM_DATA]
🔍 ScheduleScreen - Navigating to semester selection with classrooms: [CLASSROOMS]
```

## 🔍 ما يجب البحث عنه:

### **1. البيانات المرجعة من API:**
- هل الـ response يحتوي على `classrooms`؟
- هل كل برنامج له `classrooms` array؟
- هل الـ `classrooms` تحتوي على بيانات حقيقية؟

### **2. هيكل البيانات:**
```javascript
// البيانات المتوقعة:
{
  id: number,
  nameAr: string,
  nameEn: string,
  description: string,
  duration: number,
  programType: string,
  enrollmentType: string,
  isActive: boolean,
  createdAt: string,
  updatedAt: string,
  _count: {
    trainees: number,
    classrooms: number
  },
  classrooms: [
    {
      id: number,
      name: string,
      classNumber: number,
      startDate: string,
      endDate: string,
      createdAt: string,
      updatedAt: string
    }
  ]
}
```

### **3. المشاكل المحتملة:**
- `classrooms` array فارغ `[]`
- `classrooms` غير موجود `undefined`
- `classrooms` يحتوي على static data
- الـ API لا يرجع `classrooms`

## 🚨 إذا كانت المشكلة في الـ API:

### **1. تحقق من الـ Backend:**
- تأكد من أن `/api/programs` يرجع `classrooms`
- تأكد من أن الـ database يحتوي على فصول دراسية
- تأكد من الـ relationships في الـ database

### **2. تحقق من الـ Query:**
- تأكد من أن الـ API يستدعي `classrooms` في الـ query
- تأكد من الـ `include` أو `populate` للـ `classrooms`

### **3. تحقق من الـ Response Structure:**
- تأكد من أن الـ response structure صحيح
- تأكد من أن الـ `classrooms` موجود في الـ response

## 🎯 الخطوات التالية:

### **1. بعد المراجعة:**
- إذا كانت البيانات صحيحة → النظام يعمل بشكل صحيح
- إذا كانت البيانات خاطئة → نحتاج لتحديث الـ API
- إذا كانت البيانات مفقودة → نحتاج لإضافة `classrooms` للـ API

### **2. إذا كانت المشكلة في الـ API:**
- تحديث الـ backend ليرجع `classrooms`
- إضافة `classrooms` للـ database
- تحديث الـ API response structure

### **3. إذا كانت المشكلة في الـ Frontend:**
- تحديث الـ data processing
- إضافة fallback للـ `classrooms`
- إضافة error handling

## 📋 قائمة المراجعة:

- [ ] فتح Developer Console
- [ ] فتح صفحة الجداول الدراسية
- [ ] مراجعة AuthService logs
- [ ] مراجعة ScheduleScreen logs
- [ ] مراجعة SemesterSelectionScreen logs
- [ ] التحقق من وجود `classrooms` في البيانات
- [ ] التحقق من صحة بيانات `classrooms`
- [ ] اختبار الضغط على برنامج
- [ ] اختبار الانتقال لصفحة الفصول الدراسية

---

**🔍 الآن افتح الصفحة وراقب الـ console logs وأخبرني بالنتائج!**

**سأتمكن من تحديد المشكلة بناءً على البيانات المرجعة من الـ API! 📊✨**
