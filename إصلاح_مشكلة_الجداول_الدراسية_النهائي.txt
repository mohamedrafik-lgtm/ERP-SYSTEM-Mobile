# 🔧 إصلاح مشكلة عدم ظهور قسم الجداول الدراسية في المنيو

## 🎯 المشكلة:
قسم "الجداول الدراسية" لا يظهر في منيو التنقل بين الصفحات.

## 🔧 الحلول المطبقة:

### 1. **توسيع الأذونات** ✅
- تم توسيع `allowedRoles` لتشمل جميع الأدوار مؤقتاً للتشخيص
- الآن جميع المستخدمين يمكنهم رؤية القسم

### 2. **إضافة Debug Logs** ✅
- تم إضافة debug logs شاملة في `PermissionService`
- ستظهر في Console معلومات مفصلة عن الأذونات والـ menu sections

## 🚀 خطوات الإصلاح:

### 1. **مسح الـ Cache وإعادة التشغيل**
```bash
# في Terminal
npx react-native start --reset-cache
```

### 2. **أو استخدم الـ Script الجاهز**
```bash
# Windows
clear-cache-and-restart.bat
```

### 3. **إعادة تشغيل التطبيق**
- أغلق التطبيق تماماً
- أعد فتحه من جديد

## 🔍 كيفية التحقق من الإصلاح:

### 1. **افتح التطبيق**
### 2. **افتح Developer Console (F12)**
### 3. **افتح منيو التنقل**
### 4. **راقب الـ Console logs:**

```javascript
🔍 PermissionService.getAllowedMenuSections() - Allowed screens: [...]
🔍 PermissionService.getAllowedMenuSections() - Allowed screen IDs: [...]
🔍 PermissionService.getAllowedMenuSections() - All MENU_SECTIONS: [...]
🔍 PermissionService.getAllowedMenuSections() - Filtered sections: [...]
```

### 5. **ابحث عن قسم "الجداول الدراسية"**
- يجب أن يظهر في المنيو
- يجب أن يحتوي على "الجداول الدراسية" كعنصر

## 🔍 ما يجب أن تراه في Console:

### **إذا عمل الإصلاح ✅:**
```javascript
🔍 PermissionService.getAllowedMenuSections() - All MENU_SECTIONS: [
  { title: "الرئيسية", category: "home", items: [...] },
  { title: "الإدارة الأكاديمية", category: "academic", items: [...] },
  { title: "إدارة الاختبارات", category: "exams", items: [...] },
  { title: "إدارة منصة الطلاب", category: "student_platform", items: [...] },
  { title: "الجداول الدراسية", category: "schedules", items: ["Schedules"] },
  // ... المزيد
]

🔍 PermissionService.getAllowedMenuSections() - Filtered sections: [
  // يجب أن يحتوي على قسم "الجداول الدراسية"
  { title: "الجداول الدراسية", category: "schedules", items: ["Schedules"] }
]
```

### **إذا لم يعمل الإصلاح ❌:**
```javascript
🔍 PermissionService.getAllowedMenuSections() - Allowed screen IDs: [...]
// إذا لم يحتوي على "Schedules" في القائمة
```

## 🎯 الأسباب المحتملة:

### 1. **مشكلة في الـ Cache**
- الـ Metro bundler يحتاج إعادة تشغيل
- الـ React Native cache يحتاج مسح

### 2. **مشكلة في الأذونات**
- المستخدم الحالي لا يملك الصلاحيات المطلوبة
- مشكلة في الـ role checking

### 3. **مشكلة في الـ Code**
- خطأ في الـ permissions.ts
- مشكلة في الـ PermissionService

## 🔧 إذا استمرت المشاكل:

### **الحلول الإضافية:**

1. **تحقق من الـ Console logs** - راقب الرسائل بعناية
2. **تحقق من الأذونات** - تأكد من تسجيل الدخول بحساب صحيح
3. **أعد تشغيل الـ Metro bundler** - `npx react-native start --reset-cache`
4. **أعد تشغيل التطبيق** - أغلق وأعد فتح التطبيق
5. **أرسل لي الـ logs** - سأساعدك في التشخيص

## 📱 اختبار سريع:

### **بعد إعادة التشغيل:**
1. **افتح التطبيق**
2. **افتح منيو التنقل**
3. **ابحث عن "الجداول الدراسية"**
4. **اضغط عليه**
5. **تحقق من فتح الصفحة**

---

**جرب الخطوات الآن وأرسل لي الـ Console logs! 🔍✨**

**إذا لم يعمل:**
1. **أرسل لي الـ Console logs**
2. **أخبرني بنوع المستخدم المسجل دخول**
3. **سأساعدك في التشخيص الإضافي**
